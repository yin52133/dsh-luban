# Third-party notices

This package launches an isolated Python project containing
[`browser-use` 0.13.8](https://github.com/browser-use/browser-use), distributed
under the MIT License. Its exact transitive dependency closure and hashes are
recorded in the bundled `browser-bridge/uv.lock`.

No browser-use source code is copied into this package; Luban contains only a
thin protocol and lifecycle adapter.

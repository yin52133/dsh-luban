# dsh-luban 设计文档总目录

本目录是 dsh-luban 的唯一设计权威来源（Single Source of Truth）。代码实现必须与这里的文档对应；不一致时，先改文档（走 SOP）再改代码。

## 版本记录

| 版本 | 日期       | 作者  | 变更说明                   |
| ---- | ---------- | ----- | -------------------------- |
| v0.1 | 2026-08-29 | Maintainers | 初稿：建立分册结构与 SOP   |

## 分册结构

| 分册 | 路径 | 职责 |
| --- | --- | --- |
| 01 总体 | `01-overview/` | 愿景与范围、分层总体架构、目录设计、需求追踪矩阵 |
| 02 原则 | `02-principles/` | 设计原则专章（分层、契约、复用三档、许可合规） |
| 03 模块 | `03-modules/` | 12 个模块的详细设计（功能/流程图/接口/数据模型） |
| 04 接口 | `04-interfaces/` | 跨模块契约（TypeScript 签名）、公共数据模型、事件总线 |
| 05 部署 | `05-deployment/` | Windows / Ubuntu 双端部署与运行设计 |
| 06 发布 | `06-release/` | 发布原则与安全红线（README/版本/tag/npm/敏感信息） |
| 07 参考 | `07-references/` | 参考项目分析与复用档位（license 合规台账） |
| 模板 | `TEMPLATE.md` | 新文档起点模板 |

## 编号规则

- **需求编号** `R01-R11`：来自最初需求清单，永久固定，不复用。
- **模块编号** `M01-M12`：一个模块 = 一个自研插件包（或发布基础设施），模块与插件包一一对应。
- **功能编号** `M<NN>-F<NNN>`：模块内功能点，只增不改不复用；废弃标记 `dropped` 不删行。
- **里程碑** `MS1-MS4`：排序见 checklist.json，与优先级（P0 最高）共同决定开发顺序。

## 新增需求 SOP

1. **立项**：在 `01-overview/vision.md` 的需求清单追加 `R<NN>`（新编号），写清验收口径。
2. **映射**：判断归属模块。落入现有模块 → 在该模块文档「功能清单」追加 F 条目；新模块 → 复制 `TEMPLATE.md` 新建 `M<NN>-<短名>.md` 并更新总体架构的模块矩阵。
3. **接口**：若产生新的跨模块契约，更新 `04-interfaces/`；仅模块内部的接口写在模块文档内。
4. **追踪**：同步 `01-overview/trace-matrix.md` 与 `checklist.json`（新增 feature 条目，状态 `todo`，标里程碑）。
5. **自检**：运行 `node scripts/validate-design.mjs`，必须全绿。
6. **提交**：commit message 用 `docs(<模块>): <一句话>`；每个被修改的文档追加版本记录行。

## 文档纪律

- 每份文档**必须**有版本记录表，每次修改追加一行（校验脚本强制）。
- 流程图用 Mermaid 且保证语法可渲染（GitHub 原生渲染）。
- 设计先行：代码 PR 必须引用对应的 F 编号；发现设计与实现不符时，先提文档变更。
- 敏感信息（key、密码、内网地址细节）**禁止**出现在任何文档中，见 `06-release/release-principles.md`。
